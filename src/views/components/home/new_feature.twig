{# <h1>{{ component.slider_height }}</h1> #}
 {# <div class="slider" style="height : {{ component.slider_height }}px " > #}
 <div class="slider"  >
{% for slider in component.slider_collection %}
 
  <div class="slide" >

    <img src="{{ slider.slider_image_1 }}" alt="{{ slider.slider_title_1 }}">
    <div class="caption">{{ slider.slider_title_1 }}</div>
  </div>
  <!-- أضف المزيد من الشرائح -->
{% endfor %}
</div>
<style>
.slider {
  position: relative;
  width: 80%;
  max-width: 1200px;
  margin: 0 auto;
  overflow: hidden;
  
}

.slide {
  display: none;
  position: relative;
  height:250px;
}

.slide img {
  width: 100%;
  height: 100%;
  object-fit: contain;
  /*border-radius: 10px; /* لإضافة زوايا دائرية */
}

.caption {
  position: absolute;
  bottom: 20px;
  left: 20px;
  color: #fff;
  background-color: rgba(0, 0, 0, 0.5); /* لإضافة شفافية */
  padding: 10px;
  border-radius: 5px;
}

.active {
  display: block;
}

</style>
<script>
let currentSlide = 0;
const slides = document.querySelectorAll('.slide');

function showSlide(index) {
  slides.forEach((slide, i) => {
    slide.classList.remove('active');
    if (i === index) {
      slide.classList.add('active');
    }
  });
}

function nextSlide() {
  currentSlide = (currentSlide + 1) % slides.length;
  showSlide(currentSlide);
}

setInterval(nextSlide, 3000); // تغيير الشريحة كل 3 ثوانٍ
</script>

